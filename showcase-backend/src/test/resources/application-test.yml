spring:
  datasource:
    url: jdbc:h2:mem:moodinvestor;MODE=MySQL;NON_KEYWORDS=DAY;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  REDACTED_FOR_SHOWCASE
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  sql:
    init:
      mode: never
logging:
  level:
    org.hibernate.SQL: warn
