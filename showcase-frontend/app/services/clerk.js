// /frontend/app/services/clerk.js
// Minimal helper to ensure Clerk is loaded + initialized before the router runs.

function wait(ms) {
  // Logic abstracted for showcase purposes.
}

let readyPromise = null;

export async function ensureClerkReady() {
  // Logic abstracted for showcase purposes.
}

export function isSignedIn() {
  // Logic abstracted for showcase purposes.
}

export function getClerkUser() {
  // Logic abstracted for showcase purposes.
}
